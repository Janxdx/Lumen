node_modules/
dist/
.DS_Store
*.local
*.log

# credentials — .env.example is the one that belongs in git
.env
.env.*
!.env.example

# vite writes these next to the config while resolving it
vite.config.ts.timestamp-*.mjs

# wrangler local state (D1 and R2 live here during `wrangler dev`)
.wrangler/

# D1 dumps written by scripts/rename-to-soluna.sh — a copy of every account,
# every session token hash and every stored credential. Backups, not source.
*-backup-*.sql
